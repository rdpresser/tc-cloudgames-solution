apiVersion: v1
kind: Service
metadata:
  name: user-api
  labels:
    app.kubernetes.io/name: user-api
  annotations:
    # ArgoCD service link - displayed in UI under Services tab
    service.argocd.argoproj.io/url: 'http://cloudgames.local/user'
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: user-api
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http