apiVersion: v1
kind: Service
metadata:
  name: user-api
  labels:
    app.kubernetes.io/name: user-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: user-api
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http