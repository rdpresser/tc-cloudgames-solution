apiVersion: v1
kind: Service
metadata:
  name: games-api
  labels:
    app.kubernetes.io/name: games-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: games-api
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http